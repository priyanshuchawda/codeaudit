{
  "name": "codeaudit-workspace",
  "version": "0.1.2",
  "private": true,
  "description": "Skills-driven MCP system for project detection, audits, documentation evidence, and PR planning.",
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "build": "pnpm --filter codeaudit build",
    "dev": "pnpm --filter codeaudit dev",
    "dev:http": "pnpm --filter codeaudit dev:http",
    "start:http": "pnpm --filter codeaudit start:http",
    "typecheck": "pnpm --filter codeaudit typecheck",
    "test": "pnpm --filter codeaudit test",
    "lint": "pnpm --filter codeaudit lint",
    "format": "prettier --write .",
    "check": "pnpm typecheck && pnpm lint && pnpm test"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
